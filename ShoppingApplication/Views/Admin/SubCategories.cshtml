@using ShoppingApplication.Models
@model List<SubCategory>
@{
    ViewBag.Title = "SubCategories";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="container">
    <h2>Sub Categories</h2>
    <p>
        @Html.ActionLink("Add New SubCategory", "AddSubCategory")
    </p>
    <table class="table table-bordered table-hover table-responsive">
        <thead>
            <tr>
                <th>
                    Sub Category Id
                </th>
                <th>
                    Sub Category Name
                </th>
                <th>
                    Category Name
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.Category.Name
                </td>
                <td>
                    <a href="/Admin/EditSubCategory/@item.Id">Edit</a> |
                    <a href="/Admin/DeleteSubCategory/@item.Id">Delete</a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>
